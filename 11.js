let one = 1;
let two = 2;
let three = 3;
let four = 4;
one =one* 2;  // Double one
two = two*2;  // Double two
three =three*3;  // Multiply 3
four = four + 4;  // Increase  4
let sum = one + two + three + four;
console.log(sum);  
